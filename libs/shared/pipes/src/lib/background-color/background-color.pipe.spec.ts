import { BackgroundColorPipe } from './background-color.pipe';

describe('BackgroundColorPipe', () => {
  it('create an instance', () => {
    const pipe = new BackgroundColorPipe();
    expect(pipe).toBeTruthy();
  });
});
