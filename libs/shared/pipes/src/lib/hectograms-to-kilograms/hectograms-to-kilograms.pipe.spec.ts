import { HectogramsToKilogramsPipe } from './hectograms-to-kilograms.pipe';

describe('HectogramsToKilogramsPipe', () => {
  it('create an instance', () => {
    const pipe = new HectogramsToKilogramsPipe();
    expect(pipe).toBeTruthy();
  });
});
