import { ConvertStatsToUpperCasePipe } from './convert-stats-to-upper-case.pipe';

describe('ConvertStatsToUpperCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertStatsToUpperCasePipe();
    expect(pipe).toBeTruthy();
  });
});
