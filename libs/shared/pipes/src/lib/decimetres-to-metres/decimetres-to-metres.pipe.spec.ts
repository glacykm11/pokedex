import { DecimetresToMetresPipe } from './decimetres-to-metres.pipe';

describe('DecimetresToMetresPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimetresToMetresPipe();
    expect(pipe).toBeTruthy();
  });
});
